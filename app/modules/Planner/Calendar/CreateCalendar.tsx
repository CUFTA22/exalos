import Card from '@lib/Card/Card';
import styles from './Calendar.module.scss';

const CreateCalendar = () => {
  return <Card className={styles.calendar}>cl</Card>;
};

export default CreateCalendar;
